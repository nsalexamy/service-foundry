= Install Feast Feature Server on K8s

== Prerequisites

* Kubernetes cluster
* Helm 3
* Feast Feature Server chart

== Feature Store Configuration

[source,yaml]
----


== Installation

```bash
helm upgrade --install feast-feature-server ./feast-feature-server \
  --namespace service-foundry \
  --create-namespace \
  --values custom-values.yaml
```